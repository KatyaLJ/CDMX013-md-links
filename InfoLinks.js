const path = require('path')

const extMd=(extPath(directoryPath));
if (extMd=='.md'){
 console.log(readFile(directoryPath)); 
}else {
  console.log("el archivo no es .md");
};

const infoLinks = extMD.map(function())